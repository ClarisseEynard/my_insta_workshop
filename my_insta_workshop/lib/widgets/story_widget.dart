import 'package:flutter/material.dart';

// Complete the StoryWidget class

// You will need to use the following widgets:
// Column, Padding, Stack, Container, Border, CircleStory, Text

// You first need to make the CircleStory widget

class StoryWidget extends StatelessWidget {
  final String username;
  const StoryWidget({super.key, required this.username});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
